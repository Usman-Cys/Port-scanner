# Core dependencies
rich>=13.0.0          # Pretty terminal output (tables, colors)
tabulate>=0.9.0       # Fallback table formatter

# Optional: stealth SYN scanning (requires root/admin)
# scapy>=2.5.0        # Uncomment for --syn mode (raw socket support)

# Development / testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Type checking (optional)
mypy>=1.0.0
